<Window x:Class="CodexTempo.CloseChoiceDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="关闭 Codex Tempo"
        Width="356" Height="190"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        FontFamily="Microsoft YaHei UI">
  <Window.Resources>
    <DropShadowEffect x:Key="DialogShadow" BlurRadius="24" ShadowDepth="6"
                      Opacity=".22" Color="#22372C"/>
    <Style x:Key="ChoiceButton" TargetType="Button">
      <Setter Property="Height" Value="36"/>
      <Setter Property="Padding" Value="16,0"/>
      <Setter Property="FontSize" Value="12"/>
      <Setter Property="FontWeight" Value="SemiBold"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="ButtonShell"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="10">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="ButtonShell" Property="Opacity" Value=".88"/>
              </Trigger>
              <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="ButtonShell" Property="Opacity" Value=".72"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </Window.Resources>

  <Border Margin="16" Padding="22,18,22,18"
          CornerRadius="18" Background="#FAF9F4"
          BorderBrush="#80FFFFFF" BorderThickness="1"
          Effect="{StaticResource DialogShadow}">
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
      </Grid.RowDefinitions>

      <TextBlock Text="要把 Codex Tempo 放到哪里？"
                 FontSize="17" FontWeight="SemiBold" Foreground="#24312A"/>
      <TextBlock Grid.Row="1" Margin="0,8,0,0"
                 Text="隐藏后仍会在托盘运行，双击托盘图标即可恢复。"
                 FontSize="11" Foreground="#778078"/>

      <Grid Grid.Row="2" Margin="0,18,0,0" VerticalAlignment="Bottom">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*"/>
          <ColumnDefinition Width="10"/>
          <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Button Style="{StaticResource ChoiceButton}"
                Background="#607A66" Foreground="White"
                Content="隐藏到托盘" Click="HideButton_Click"/>
        <Button Grid.Column="2" Style="{StaticResource ChoiceButton}"
                Background="#E9E9E3" Foreground="#3F4942"
                Content="退出程序" Click="ExitButton_Click"/>
      </Grid>
    </Grid>
  </Border>
</Window>
